# Python analyzer

Owns Python parsing, bindings, call/name resolution, typed facets, exact source
evidence, normalized fingerprints, and Python-specific poison/golden corpora. It does
not own canonical identity or serving indexes.

Current extraction source: `analyzers/python_syntax.py`.
