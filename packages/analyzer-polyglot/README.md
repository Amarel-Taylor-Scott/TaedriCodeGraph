# Polyglot analyzers

Owns language inventory plus Tree-sitter, SCIP, CodeQL, and Code Property Graph import
adapters. Imported semantics retain their producer, version, scope, evidence, and
coverage instead of being flattened into synthetic Python-like facts.

Current extraction source: `sources.py`.
