# Primitive factory

Owns language-adapter outputs that turn existing source entities into registry-native
candidate capsules, contracts, graph neighborhoods, and search descriptors. Factories
never import or execute target code and never promote their own output.

The active reference implementation currently lives in
`src/taedri_codegraph/primitive_factory.py` while the monorepo is transitional.
