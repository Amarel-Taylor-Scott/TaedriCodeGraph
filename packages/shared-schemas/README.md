# Shared schemas

Owns namespaced descriptor, predicate, role, value, provider, view, and index-manifest
schemas plus conformance fixtures and generated public artifacts. Published
key/version pairs are immutable.

Current extraction sources: `registry.py` and the registries in
`representations.py`.
