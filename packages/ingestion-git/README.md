# Git ingestion

Owns repository, commit, tree, blob, submodule, tag, fork, and release acquisition
receipts. It will preserve Git object identity while mapping snapshots into the
language-neutral kernel.
