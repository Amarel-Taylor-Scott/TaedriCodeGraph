# PyPI ingestion

Owns Simple API acquisition receipts, artifact pinning, safe wheel verification,
distribution/import namespace separation, metadata, license, SBOM, and provenance
extraction. Target packages are never imported during static ingestion.

Current extraction source: `artifacts.py`.
