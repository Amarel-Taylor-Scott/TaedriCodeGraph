# Compatibility

Owns directional typed contracts, sparse candidate blocking, solver outcomes, adapter
witnesses, unknown handling, verification requirements, and evidence-backed receipts.
It must not materialize all pairs.

Current extraction source: `compatibility.py`.
