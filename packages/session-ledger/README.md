# Session ledger

Owns privacy modes and append-only receipts for coding-harness requests, retrieval,
selection, materialization, model attempts, verification, acceptance, abstention, and
closure. Raw prompts and source bodies are excluded by default.

The active reference implementation currently lives in
`src/taedri_codegraph/sessions.py`.
