# numeric-mean primitive

`numeric_mean(values)` validates a non-empty finite numeric vector and computes its floating-point arithmetic mean with `math.fsum` for improved summation accuracy.

Reference semantics:
- https://docs.python.org/3.12/library/statistics.html

This is a complete, dependency-free, deterministic capsule: source, contract, examples, tests, runtime lock, license evidence, provenance, interface graph, and searchable capability labels are all present and independently validated.
